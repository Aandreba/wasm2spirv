CC := "gcc"
DIR := justfile_directory()
WORKSPACE := DIR / "../.."

build:
    cargo build --release

test: build
    {{CC}} {{DIR}}/tests/saxpy.c --std=c11 -I{{DIR}}/include -L{{WORKSPACE}}/target/release/ -lwasm2spirv_c -o {{DIR}}/tests/saxpy -lc -lm -static
    {{DIR}}/tests/saxpy
